import * as React from 'react-md';
import * as GoogleMapReact from 'google-map-react';
import { CardText, TabsContainer, Tabs, Tab, TextField, Button } from 'react-md';

export interface AdminViewState {

}

export interface AdminViewProps {

}
export class AdminView extends React.Component<AdminViewProps, AdminViewState> {
    public constructor(props: AdminViewProps) {
        super(props);
    }

}