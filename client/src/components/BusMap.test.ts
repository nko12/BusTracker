import * as React from 'react';
import { mount } from 'enzyme';
import BusMap  from '../components/BusMap'

describe('BusMap', () => {

    it('should always have a GoogleMap root element.', () => {
        
    });
});